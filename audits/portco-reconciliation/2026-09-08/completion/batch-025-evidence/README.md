# Batch 025 — two evidence-bound seed-only rechecks

Lighthouse (142) and Permanent Power Company (181) were previously verified. Whole-population closeout identified missing historical attribution overlays. The explicit recheck preserves both prior completion records, all consumed receipts and all terminal source tasks. The new release must pass every ordinary verification gate before either name counts as verified again.

Both records classify as SEED_ONLY. Complete canonical images and all five current/former owner before-images matched the fresh read-only snapshot. The evaluated seed-identity proof checks every owner, including preserved owners. Two deterministic historical overlays are inserted; no company, organization, ownership period, fund link, stake, date or production metadata changes. The existing seed-only rationale for other CIM entities explicitly coexists with the unchanged null production rationale.

Lighthouse reuses the exact accepted Excelsior seller HTML and capture provenance. The normalized capture wrapper does not claim a fresh retrieval. Its current BlackRock owner remains unchanged and retains its previously verified qualifications.

Permanent Power reuses the accepted SEC Q1 filing. Missing bytes for the previously cited CIM Aquamarine page were fetched once. That page confirms CIM and an unnamed joint-venture partner obtained acquisition rights in 2014; it does not identify a particular fund or partner. The historical attribution remains UNRESOLVED. No ownership fact is inferred from the gap.

The other-CIM seed explanation is unchanged. Its citation list is narrowed to the verified Q1 primary filing used for this decision; the prior Q4 secondary reference remains preserved in the frozen seed before-image. Production citations are unchanged.

The shared offline checker validated the original proposal/approval/receipt chains, every owner, source bytes and 24-release lineage. All 2,725 bound files remain hash-checked. Full evidence remains on disk; only compact results are reported. Publication copies passed the shared secret scan without requiring redaction.

No database apply manifest was emitted. This release must never dispatch a database apply. Protected main deployment and complete after-image, seed, API, redirect, ownership, citation and affected rendered-card verification remain required. Preparation is not completion.
