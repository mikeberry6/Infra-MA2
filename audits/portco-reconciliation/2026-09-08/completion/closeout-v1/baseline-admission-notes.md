# Baseline-only scope: evidence and next implementation

Implemented locally, not yet released: scripts/portco-completion/baseline-provenance.ts is a pure shared historical-identity validator using existing schema/hash/dataset/source validators. It returns source-order keys and explicit ownershipVerified:false, metadataVerified:false, writeAuthorized:false. It does not read production, admit register names, compile proposals or apply writes. Eight targeted tests, final strict tool typecheck and ESLint pass. Logs are /private/tmp/portco-baseline-provenance-{tests,typecheck,eslint}.log. Keep this future code out of already-merged batch029; integrate with a distinct admission/provenance variant only after029 fully verifies, then run final-content regression and protected checks.

59 names have no execution task because the original ledger records NO_CHANGE / VERIFY_NO_CHANGE. This is not a missing transaction receipt: no original apply was required. baseline-scope-bindings.json proves unique canonical IDs, names, seed keys and historical dispositions (50 census matches, 9 exact retained-unlinked agreements). baseline-dataset-bindings.json verifies their original production/seed dataset hashes and all 11 comparable identity/relation-count fields. The datasets do not contain complete owner images. Neither artifact proves current ownership or authorizes metadata writes.

After batch029 completes, add a distinct evidence-bound baseline provenance/admission path to the shared checker. Preserve the existing receipt-required path unchanged. Bind authoritative idle progress/source boundaries, original ledger and both dataset hashes, original row hashes, full current observed company/owner/redirect/fund images, direct source bytes and every complete owner decision. An old baseline identity match must never masquerade as an applied proposal or immutable receipt. Use one fresh target-pinned read-only snapshot after offline validation, then all ordinary protected release, atomic apply, after-image, seed, API and rendered checks. New names start REMAINING. No manual register edits, source-task reopening, inferred facts or unsupported mutations.

Selection must follow original source order: first managerIndex and original census-row index for the 50 census-bound names, then original repo-row index for the nine explicitly retained unlinked names. Do not use alphabetical canonical-row order or pretend these indexes are original execution task numbers. Store explicit provenance/order keys; test complete earliest-ten selection, omission, duplicate identity, ambiguous seed, stale hash, partial owner coverage, forbidden synthetic receipt, active/frozen release and replay continuity. A precise park remains unverified.

First ten under that order: Arevon Energy, Inc.; FirstEnergy Transmission, LLC; Sempra Infrastructure Partners, LP; Alberta Schools Alternative Procurement I; Conterra Networks; IonicBlue; US Wind; Lincoln Terminal Holdings; MXT Holdings; InTransit BC. This is navigation only until a fresh sealed admission reproduces the full mapping.

Negative finding to reuse: the original census-only scope assertion failed for nine names, first Captis Energy. All nine have zero census and repo-only rows, one BOTH repo row, RETAIN_UNLINKED, exact canonical/name/seed-key agreement and the explicit rationale that production and evaluated seed agree although not linked to the 100-manager census. This was resolved by separately proving that precise disposition, not by loosening census validation. No repeated ChatGPT or broad snapshot search is needed to recover this finding.
