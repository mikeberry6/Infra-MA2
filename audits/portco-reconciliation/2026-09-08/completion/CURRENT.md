# PortCo completion — resume here

Work ONLY in `/Users/mikeberry6/Infra-MA2-portco-ipx-attribution-repair`, never the main checkout. Read `EFFICIENT_EXECUTION.md` once and run its compact status command. This locator is not proof; validate authoritative inputs for the next gate.

## Boundary

Local register: **111 verified / 56 parked / 0 remaining**, no active release or pending parked revisit. These 167 names are NOT the full population. Another 59 baseline identities need admission/review; four existing deferred exceptions and global closeout remain. All 496 original source tasks are terminal; never reopen them. No batch030 exists.

Batch029 completed locally (KAPS remains parked), zero seed/database writes and no apply receipt. Protected PR988 merged as `53a5838739d48d6dec6bc2693c522efc93b6efd5`; CI and matching canonical production verification are archived under `audits/portfolio-fund-attribution/2026-09-08/scoped/portco-completion-029`. Never repeat any prior transaction, including batch028.

## Next unfinished step

Branch `codex/portco-baseline-provenance` carries029 bookkeeping, baseline admission/replay and separate baseline decision-record provenance. All ten completion-tool suites passed (141 tests), strict typecheck and ESLint passed; logs `/private/tmp/portco-baseline-records-{tests,typecheck,eslint}.log`. No030 release is active.

Next: assemble the first ten sourced decisions using `preparation/baseline-030/REVIEW.md`. Its sealed admission and exclusive candidate are ready, but authoritative progress remains unchanged. Validate the complete decisions against the candidate before promotion; then capture one fresh target-pinned snapshot and run final-content/protected release gates. Historical identity and synthetic tests do not prove ownership. Keep exact unsupported issues parked. Implementation detail and negative findings: `closeout-v1/baseline-admission-notes.md`.

## On-demand detail

- `checkpoint-baseline-provenance.md`: exact boundary hashes, first names, known negative findings and prior check logs.
- `checkpoint-through-029.md`: previous release history.
- `scripts/portco-completion/README.md`: shared validation/release procedure; read relevant sections when needed.

Carry completion bookkeeping in the next scoped protected release. Preserve all source/hash/secret-scan/approval/exact-SHA/atomic-write/receipt/after-image/seed/API/redirect/ownership/render gates. No repeated research, full seed execution, model downgrade or unrelated changes.
