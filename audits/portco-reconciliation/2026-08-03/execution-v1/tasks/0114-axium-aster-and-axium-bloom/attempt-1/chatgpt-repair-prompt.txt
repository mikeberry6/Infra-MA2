Repair only the response format. Do not do new research and do not change any factual conclusion. Re-emit the same complete answer using the required BEGIN_JSON/END_JSON and BEGIN_REVIEW/END_REVIEW markers. The JSON must parse strictly: one minified JSON object on one physical line, no literal newlines inside strings, no unescaped control characters, all mandatory top-level keys present, no prose or UI citation labels outside the marked review, no code fence, and exactly one evidence row with isRecommendedPrimary=true. Keep it under 7,500 characters.
