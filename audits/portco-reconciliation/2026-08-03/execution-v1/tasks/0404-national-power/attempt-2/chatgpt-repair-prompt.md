Your research result is substantively complete, but the JSON is not valid because literal line breaks appeared inside string values (including headquarters, evidence purpose fields and an excluded-candidate URL). Without doing new research or changing any claim, source URL, decision or confidence, re-emit the entire packet once as strict valid minified JSON between BEGIN_JSON and END_JSON followed by the same concise BEGIN_REVIEW / END_REVIEW paragraph. Keep every mandatory key and exactly one recommended primary source, and do not put any literal newline inside a JSON string.
