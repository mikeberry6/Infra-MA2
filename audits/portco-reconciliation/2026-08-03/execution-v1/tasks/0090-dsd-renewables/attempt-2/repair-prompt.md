Formatting-only repair; do not research or change findings. Return the same complete minified fenced JSON and one bullet, with only these edits:

1. Replace every evidence `workingStatus:"OPEN"` with `workingStatus:"WORKING"`.
2. Replace every evidence `sourceTier:"TRADE"` with `sourceTier:"REPUTABLE_SECONDARY"`.
3. Replace `unresolvedQuestions` with `[]`; undisclosed stakes and the undisclosed Ares fund are nonblocking `NOT_PUBLICLY_DISCLOSED` facts, not unresolved identity or ownership.

Preserve all other values, all 16 top-level keys, four evidence rows, and exactly one recommended primary. Output only the repaired fenced JSON and one bullet. Do not truncate.
