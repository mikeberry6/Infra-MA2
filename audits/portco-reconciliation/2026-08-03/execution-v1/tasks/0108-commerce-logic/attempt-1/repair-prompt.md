Repair only the formatting and completeness of your preceding Commerce Logic answer; do not redo the research or add narration. The prior output was truncated inside `operatingResolution.footprint`, omitted mandatory keys, and used the wrong as-of date.

Return one complete minified fenced `json` object followed by exactly one short Markdown sentence. Use `asOfDate":"2026-08-18"` exactly. Include every mandatory top-level object/key from the original prompt, valid closed JSON, no citations outside the JSON, no more than six evidence rows, and exactly one `isRecommendedPrimary:true`. Keep the entire response under 4,500 characters. Preserve the evidence-based `EXCLUDED` decision and 2025-07-15 realized exit unless your already-completed research directly contradicts them. Use concise strings; noncritical unavailable facts may be `NOT_PUBLICLY_DISCLOSED`.
