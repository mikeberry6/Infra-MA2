# Task 99 retry note

The first protected production dry-run made no writes and stopped because the
NuGen PR Newswire URL already exists as a shared `Source` with label
`PR Newswire` and type `ARTICLE`. Attempt 2 preserves that exact global source
metadata. The researched Pearl/Ruby identity, ownership, portfolio boundary,
milestone, seed-retirement, and exit-search decisions are otherwise unchanged.
