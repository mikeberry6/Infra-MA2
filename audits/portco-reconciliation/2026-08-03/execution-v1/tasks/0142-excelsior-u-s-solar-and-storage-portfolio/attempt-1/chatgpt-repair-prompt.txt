Schema repair only. Reissue the exact same substantive answer with no factual changes. The prior JSON was invalid because rendered citation markers inserted literal line breaks inside JSON string tokens. Return BEGIN_JSON, one single-line valid minified JSON object with no inline citation widgets or citation markers anywhere inside the JSON and only plain-text HTTPS URLs in the evidence array, END_JSON, then BEGIN_REVIEW and the same concise review, then END_REVIEW. Use exactly one recommended primary, add no commentary, and do not browse again.

