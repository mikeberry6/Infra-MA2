Your JSON was truncated inside the Harbert owner record and is invalid. Return the complete response again as exactly one minified fenced `json` object followed by `Result recorded.` Nothing else. Preserve the requested schema and conclusions, remain under 7,000 characters, use no more than eight evidence rows and four milestones, and keep every string under 200 characters. Include all four current manager-universe owners, the exit search, before/after changes, empty `unresolvedQuestions` if identity and active ownership are resolved, and exactly one recommended primary source.
