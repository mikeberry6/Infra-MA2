# Ten-name PortCo completion

This internal workflow replaces per-company audit tooling for subsequent work. It does not change the source-task state machine, the five-member source-bundle schema, public data interfaces or the protected attribution write scope.

## Boundaries

Work only in `/Users/mikeberry6/Infra-MA2-portco-ipx-attribution-repair`. Start from fresh protected `origin/main`, preserving unrelated local artifacts. PR958 is verified; its completion bookkeeping accompanies the first combined release.

The progress register is keyed by canonical company ID, not by fields or duplicate source tasks. Initialize it once using `reviewedInventory()` from the frozen chronology, completed source proposals and published authority reports. It deliberately starts with zero fully verified names: an old audit report is not a correction receipt. Original unavailable-fact exceptions remain in the source manifest and are not silently counted as new work or as resolved facts.

`nextNames()` selects ten reviewed backlog companies in original source order before unreviewed names. Fewer are allowed only when that eligible pool is exhausted. Parked companies are revisited after the main pass. One active release is enforced independently of the permanently idle, terminal source ledger. No source tasks are reopened.

For the parked pass, set `phase: "PARKED_REVISIT"` in the small-record preparation configuration. This phase is included in the sealed batch, checked offline before capture, and replayed by the same completion-lineage validator. It cannot start while any main-pass name remains. Select the earliest ten not-yet-revisited parked names in original source order, regardless of evidence availability. Only successful protected completion records `parkedReview` with the exact prior issue, batch ID and completion-file hash. A name that remains parked retains its current precise issue and is skipped for the rest of this pass; it never increments the verified count. Frozen/unfinished reviews do not advance the queue. A later attempt at a reviewed exception requires a separately authorized, evidence-bound follow-up, not removing its marker or reopening source tasks. Immutable main-pass batches omit this optional phase and retain their original hashes and replay results.

## Preparation

The global closeout may identify already-completed source companies absent from the original review register. `prepare-extension` admits the earliest ten such names through `PORTCO_COMPLETION_INVENTORY_EXTENSION`, bound to exact idle progress, seed, terminal execution manifest, idle source ledger and complete scope-diagnostic bytes. It revalidates every completed source proposal/approval/receipt chain (including nested receipts), reconstructs the complete diagnostic-to-company mapping and rejects omissions, duplicate bindings, reordered selection and canonical-name drift. Existing names, original universe, consumed receipts and all prior completions remain unchanged; additions begin REMAINING, never VERIFIED. Missing completed-source bindings require separate scope adjudication and cannot enter through this path.

An extension only writes an exclusive preparation candidate, not the authoritative register or database. Assemble the selected decision records before promoting it. Full completed-release replay reproduces each extension from its immutable inputs; neither a diagnostic nor an old receipt authorizes another write. The ordinary complete-owner, source, snapshot, release and verification gates remain mandatory. Use `cli.ts prepare-extension --progress=<progress.json> --request=<extension-request.json> --seed=prisma/seed-data/ownership-attributions.manifest.json --output=<new-directory>`.

The separate `prepare-baseline-admission` command prepares `PORTCO_BASELINE_ADMISSION` candidates for the explicitly published 59-company historical NO_CHANGE scope. It never fabricates an original apply receipt or reopens a source task. It binds exact idle progress/seed/source boundaries, original ledger and both original datasets, and immutable published diagnostic/scope bytes. Rehashed partial scope, ambiguous identities, altered dispositions, missing evidence, pending work, active/frozen releases, skipped names and duplicate admission are fatal. New names start REMAINING and preserve all existing history. Source order is census manager/row first, then explicitly retained-unlinked repo-row order; the stored baseline ordering slots are not execution task numbers. Replay must reproduce the exact admission from original evidence. This is historical identity proof only, not current ownership proof or write authority.

Use `cli.ts prepare-baseline-admission --progress=<progress.json> --request=<baseline-admission.json> --seed=prisma/seed-data/ownership-attributions.manifest.json --output=<new-directory>`. Outputs are exclusive candidates only; the authoritative register is unchanged. **Do not promote baseline candidates yet:** the distinct baseline decision-record provenance path still needs integration and tests before these names can pass full preparation. The legacy receipt-required path remains unchanged. Assemble sourced decisions with full current owner/company/redirect/fund images and all ordinary protected release/verification gates before admission is used operationally.

A newly evidenced gap in a previously verified company uses an explicit `PORTCO_COMPLETION_RECHECK`, never a manual progress edit or a replayed transaction. This is allowed only after the main and parked passes are idle and fully traversed. The request binds the exact current progress and seed hashes, up to ten exact prior verified records (including prior completion and parked-review history), a diagnostic file and reason for each. `recheckProgress()` preserves the whole prior record in append-only `recheckHistory`, keeps all completed batch IDs and consumed receipts, and requeues only those names in source order. The full ordinary preparation/release/verification gates run again with a new batch ID. It cannot reopen a parked issue, a source task or an active/unfinished release; it does not approve a mutation or accept the diagnostic as source truth.

`prepare-recheck` checks the exact evidence bytes and terminal idle source boundary and writes an exclusive candidate directory only. It never changes the authoritative register, seed or database. Assemble sourced decisions before promoting a candidate. Completed-release replay reconstructs every intervening recheck from its exact before-progress, unchanged seed and evidence bytes; unexplained edits, missing lineage or altered evidence remain fatal. Legacy releases omit `recheckHistory` and keep their existing hashes. A rechecked name is not counted as verified until its new full completion succeeds.

1. Use small `decisionSchema` records. Bind existing authority/receipt/source files by byte SHA; exactly one primary citation per owner decision. Review only unresolved facts or changed dependencies. No repeated ChatGPT requests. Preserve evidence qualifications and legal-vehicle/manager distinctions.
2. Validate files offline before any production capture. `scanPublication` rejects known credential formats without echoing values; GitHub secret scanning remains required. Keep raw originals local when necessary. `verifyRedaction` checks exact replacement counts plus original/publication hashes; never publish the original literal token or change substantive evidence. Do not automatically sanitize source evidence.
3. Capture all selected company images, owner metadata/IDs, redirects and linked/target fund dependencies in one target-pinned RepeatableRead, READ ONLY transaction using `capture()`. Preserve the output exclusively. A successful snapshot is not recaptured to hide a failed comparison. Rebind stale inputs only after reviewing the change.
4. Build one batch manifest with complete before-images and dependency hashes. `compileBatch()` validates it using the existing attribution seed and apply schemas. It emits compatible seed, exact projected company after-images and the existing apply manifest format, or no apply manifest for seed-only/no-op batches. It never invokes the full seed runner or writes a database.
5. Unsupported identity/organization/vehicle/stake/year changes, unresolved conflicts and missing seed identity bindings are parked with exact required work. A parked company has zero mutations. A safe company must account for every owner. Metadata not being changed is preserved exactly; do not declare unresolved non-target issues fully verified.

The rationale for a substantive production correction must identify the factual defect, not stylistic preference. For equivalent wording retain the source-supported production text and align seed only. The shared checker cannot itself adjudicate source truth: the reviewed decision records remain part of the protected PR.

When production has a null rationale, a decision may retain that exact state and provide an explicit `seedOnlyRationale` to satisfy the existing seed schema's nonempty explanation requirement. This is allowed only with `productionChange: NONE` and an unchanged complete owner state; it cannot accompany any production metadata or fund-link change. The annotation still requires the same direct evidence and one primary citation. Record this intentional text difference rather than claiming byte-identical rationale parity. It is not an exception for conflicting fund, owner, vehicle, stake, year or identity facts.

Historical attribution uses an optional `seedIdentity` proof, never removal of the legacy guard. `captureSeedIdentity(root, selected)` in `seed-identity-files.ts` reads the actual evaluated company exports and existing ownership/organization resolvers without calling `seedDatabase`. The proof hashes the entire evaluated company collection, each selected complete company, every raw owner and resolved organization/vehicle/state, and all seven company-input/resolver files. `prepare-records.ts` accepts the proof as an optional path/hash in its configuration, checks it offline, and seals it into the batch with its dependencies. The filesystem gate and protected transaction sidecar independently re-evaluate it. Completed release replay also requires that exact proof; changed evaluated inputs fail closed rather than silently rebinding an old identity. Immutable legacy batches omit this field and keep their original behavior and hashes.

For an opted-in company, every production owner, including preserved owners, must have one exact evaluated seed counterpart, with no extra seed owners. Both the full overlay key and the seed runner's coarser organization/vehicle/year dedupe key must be unique. Organization identity, literal vehicle, stake, investment year, exit year, active state and transaction state must match the frozen production image. Existing overlay rows retain their record IDs and exact expected hashes. Only a missing **historical attribution overlay** may use `expectedSeedSha256: null`: its existing former owner must be proven, its complete key and deterministic `OFA-HIST-` record ID must be absent from the full seed before-image, and the compiler inserts only the metadata overlay and updates its count/hash. This does not create an ownership period. The effective seed after-state is checked using the seed runner's inline-value precedence; an inline override cannot hide a failed correction. Fund links still require existing target dependencies and cannot change the owner/manager. All complete after-image and rollback guards remain required. Unsupported company, organization, chronology, legal-vehicle, stake or canonical-text work stays parked.

The proof explicitly selects companies requiring evaluated historical identity checks; it cannot include unselected or parked companies. A mixed batch may also contain ordinary active-owner decisions under the unchanged legacy guards. Omitting a company from the proof does not permit historical patches or absent-overlay insertion for that company.

New snapshots also capture the actual published fund-name catalog in the same read-only transaction, separately from selected fund-link dependencies (which may legitimately be empty). The compatibility adapter validates against the existing protected snapshot schema locally; it never invents a fund dependency or relaxes the nonempty catalog requirement. The transaction guard compares the captured catalog before and after writes, and completion checks it again. Legacy immutable snapshots retain their original hashes and compatibility behavior when their dependency catalog is nonempty.

For names without a legacy field-authority report, use `decision-records.ts` and `prepare-records.ts`. Each small data record binds the original completed proposal, approval and immutable receipt (including its batch member index), the complete original canonical semantic image, every current or former owner before-image, existing research, and exact source bytes/capture metadata. The offline pass verifies the original chain and exact next eligible group before importing the production reader. Source captures with failed HTTP status or zero bytes are not accepted for mutations; a successful status alone is not proof of the substantive claim. A rotating page, JavaScript shell or challenge response must not substitute for the cited evidence. Parked records preserve all owners and bind the precise gap and available evidence without patches.

If a canonical difference itself prevents that comparison, a zero-mutation PARKED record may additionally bind `parkedObservedImage`: exact complete observed company bytes, unchanged canonical ID/name, all observed owner IDs and a precise issue. The original applied proposal/approval/receipt remains mandatory and preserved. Without the receipt-proven attribution lineage described below, only this explicit parked form can bind a different observed semantic image. The single fresh snapshot must match the observed image and every full owner hash. This records drift without authorizing any correction or treating the company as verified.

A recorded intervening attribution transaction can instead be bound through `attributionImageLineage`: one exact observed snapshot and chronologically ordered manifest/approval/immutable-receipt chains after the original apply. The checker reverses only receipt-proven fund links and their derived manager-name projection, using the same resolver as the actual Prisma image loader. Complete attribution after-states, every observed owner hash, literal ownership identities, receipt chronology and prior fund-manager dependencies must match; the restored complete canonical semantic image must equal the original approved image. Unexplained changes remain fatal. This path cannot be combined with the parked-image exception and is provenance, not source truth or mutation authorization. Every chain file remains batch-bound, fresh snapshot comparison is unchanged, and source adjudication, full seed identity, protected release, atomic apply and complete after-image verification still apply. Unlinking a fund projects the existing organization's name; it does not rename that organization.

The compiler accepts that unlink-derived label difference only with the complete evaluated seed identity proof, an existing production organization ID, and an exact seed investment-firm/production organization-name match. It preserves the organization ID/name and every ownership field and projects the after-image with the actual loader's resolver. Missing proof and changed-manager links to another fund still fail closed. This is not permission to harmonize stored names or infer organization equivalence.

Run `npx tsx scripts/portco-completion/prepare-records.ts --config=<records-config.json> --output=<new-scope> --expected-sha=<main-sha> --offline=true` first. The configuration binds the idle progress hash, up to ten record paths/hashes, and every preceding completed release. After it passes, run the same command without `--offline=true` using the target-pinned read-only environment. The single captured snapshot must match the original canonical semantic image and all owner hashes, including preserved owners. Any drift fails closed and leaves the snapshot for explicit recovery; `--reuse-snapshot-sha` never recaptures it. Generated active progress is only a candidate, not automatic promotion of the authoritative register or production approval.

Complementary historical authority packets may be combined only through an explicit `combinations` entry naming the canonical company, every exact report SHA and the audited reason. `reviewed-packets.ts` normalizes row-array and single-owner envelopes while retaining each original report, dependency and source binding. It rejects duplicate or overlapping owners/seed records, conflicting recommendations and incompatible source boundaries. Preparation additionally requires identical complete canonical company images across all packets and compares every owner before-image to the single fresh production snapshot. This is a disjoint union of existing decisions, not inferred fact reconciliation or permission to drop a packet.

An explicit `historicalTests` binding may preserve a report's original test bytes from an archived audit fixture while also binding the current test and historical reader. The checker permits only the exact `node:fs` to frozen-reader import migration; every assertion and all other bytes must be unchanged. Arbitrary stale code or data dependencies still fail closed.

`prepare-reviewed.ts` accepts a `completedReleases` list of immutable completion directories and receipt paths. `completedSeedLineage()` replays every preceding batch through the existing compiler, receipt-chain and completion validators, requiring consecutive progress and seed hashes and the exact current idle register. Only then may an older seed dependency be rebound to its frozen before-copy, and only when every selected company's seed record is byte-semantically unchanged. A changed selected record, missing receipt, omitted release or unexplained seed change fails closed. Original authority reports and hashes are never rewritten. Both historical single-image and multi-image snapshot envelopes are supported; their entire original payload hash and selected complete image remain mandatory.

## Release and verification

One scoped PR combines checker changes when needed, decisions, compatible seed overlay, reviewed apply manifest and previous release bookkeeping. Run targeted checks during preparation, the full regression suite once on final contents, offline seed validation, and required GitHub build checks. Do not create a separate preliminary audit deployment.

Use the existing `.github/workflows/portfolio-fund-attribution-apply.yml` and protected approval for supported mutations. Its mutation list has no five-company limit. Require exact protected merge SHA on the canonical Git-integrated production alias, not a preview, before dispatch. Keep immutable workflow receipts. Never retry a dispatched transaction merely because the response was lost. Persist APPLYING before dispatch; unknown outcome or any post-apply failure becomes VERIFYING_FAILED. Recover from immutable receipts and complete after-images, not a repeated apply. Existing transaction rollback/serialization tests and workflow checks must continue to pass.

`complete()` requires exact canonical release proof, all complete projected company/owner/fund/redirect after-images, a verified manifest/approval/production receipt chain for writes, exact receipt before-images and unused receipt hash. Every safe name additionally requires durable seed alignment, API, redirects, ownership, citations and rendered-card evidence. Route smoke alone is not a rendered card. Seed-only/no-op batches must not carry a DB receipt. Completion artifact files must exist and match their hashes before register finalization. Failures freeze the batch until the same release is fully recovered.

Store completion evidence durably. Carry release bookkeeping in the next combined batch where safe; publish a final closeout release if no next batch remains. Report fully verified / parked / remaining names separately from field counts. Final reconciliation includes the original exact exceptions, full production/seed identity, ownership and redirects, not just the original 155-name field-difference inventory.

`recoverPreApply()` is a narrow exception for a failed immutable-artifact validation gate where the exact GitHub run proves every database, dry-run, apply and post-apply step was skipped. Preserve the failed release, original snapshot, archive digest and run evidence. A recovery retains the same active batch ID, every decision, original dependency, seed, terminal source boundary and complete company/owner/fund before-image; only a fresh explicitly reviewed snapshot and compatibility catalog are rebound. Unknown, started or successful transactions cannot use this path. Publish the recovery through a new protected PR and require its exact canonical merge before a new authorized dispatch. This is not a retry of an applied transaction, and cannot be used for post-apply failure recovery.

## Commands

`npx tsx scripts/portco-completion/cli.ts status --progress=<progress.json>`

`npx tsx scripts/portco-completion/cli.ts prepare-recheck --progress=<progress.json> --request=<sealed-recheck.json> --seed=prisma/seed-data/ownership-attributions.manifest.json --output=<new-candidate-directory>`

`npx tsx scripts/portco-completion/cli.ts offline --progress=<progress.json> --batch=<completion-batch.json>`

`node --env-file=<local-readonly-env> --import tsx scripts/portco-completion/cli.ts snapshot --progress=<progress.json> --batch=<completion-batch.json> --expected-sha=<full-main-sha> --output=<new-snapshot.json>`

`npx tsx scripts/portco-completion/cli.ts compile --progress=<progress.json> --batch=<completion-batch.json> --snapshot=<snapshot.json> --seed=prisma/seed-data/ownership-attributions.manifest.json --output=<new-output-directory>`

Compilation outputs are preparation artifacts, not approval or deployment. Never overwrite them with `--force`. Publication staging must be an exact file allowlist; never `git add .` in this recovery worktree.

The existing apply entry point also loads the committed completion sidecar when a ten-name release is active. It compiles the frozen inputs again, checks the entire published seed, rejects a previously successful transaction, and compares complete selected company/owner/redirect/fund images both before and after the writes inside the same serializable transaction. An after-image mismatch rolls the transaction back. This supplements, rather than replaces, protected workflow and immutable receipt validation.

Historical field-authority tests use `historical-fixtures.ts` for the original attribution seed, pinned by both byte and schema hashes. Their historical assertions remain unchanged while the batch compiler validates the new live seed overlay. Unchanged, byte-identical source evidence already committed at the pinned base is reused without republishing it; all new or modified publication copies must pass the secret scan. Existing raw originals and explicit redaction records remain preserved.

Shared regression: `npx vitest run scripts/portco-completion/*.test.ts scripts/portfolio-fund-attribution/*.test.ts`.
Strict tool typecheck: `npx tsc -p scripts/portco-completion/tsconfig.json`.
