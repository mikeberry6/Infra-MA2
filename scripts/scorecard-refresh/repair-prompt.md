Your preceding response failed the strict scorecard output contract. This is the single permitted repair for this company.

Correct only formatting, schema shape, cross-references, counts, and internally inconsistent enum/status fields. Preserve the researched facts and citations. Do not perform new research, broaden scope, change companies, or add unsupported facts.

Validation failures:

{{VALIDATION_ERRORS}}

Original response:

{{ORIGINAL_RESPONSE}}

Required JSON Schema:

```json
{{OUTPUT_JSON_SCHEMA}}
```

Return exactly one corrected `<scorecard_refresh_json>` section and one corrected `<scorecard_refresh_report>` section, with no text outside them and no `proposalHash`.
