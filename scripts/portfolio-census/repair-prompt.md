Regenerate the complete two-section response for the same manager.

Preserve researched facts and direct source URLs. Correct only the validation failures below, recompute every summary count from the arrays, and return no text outside `<portfolio_census_json>...</portfolio_census_json>` and `<portfolio_census_report>...</portfolio_census_report>`.

Validation failures:

{{VALIDATION_ERRORS}}
